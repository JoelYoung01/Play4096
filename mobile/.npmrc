# React Native / Metro works most reliably with a flat node_modules layout.
# Without this, pnpm's isolated linker hides babel-preset-expo from Babel during
# the Xcode "Bundle React Native code and images" phase (transformFile crash).
node-linker=hoisted
