engine-strict=true

# Allow non-interactive installs in Docker and other headless environments.
# Prefer setting CI=true in CI/Docker; this is a safe fallback for local image builds.
confirm-modules-purge=false
